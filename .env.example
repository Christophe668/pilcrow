# Example environment variables for the wallabag Expo client.
# Copy to a local env file (gitignored) and fill in real values for local development.
#
# This file is intentionally empty for now -- Phase 1 stores connection/credential
# values in SecureStore via the in-app onboarding flow, not in env vars.
